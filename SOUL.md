# SOUL — multi_agent

## 왜 존재하는가

하나의 모델 호출로는 품질이 흔들리는 작업(조사 → 검증 → 작성)을, **역할이 분리된 여러 에이전트의 협업**으로 더 안정적이고 검증 가능하게 만든다. 이 프로젝트는 그 협업 패턴을 가장 단순한 형태로 보여주는 레퍼런스다.

## 판단 기준

새 기능·변경을 고민할 때 다음 순서로 묻는다:

1. **책임 분리가 선명한가** — 각 에이전트는 하나의 일만 한다. 한 에이전트가 둘 이상의 책임을 지면 쪼갠다.
2. **투명한가** — 중간 산출물(research, critique)은 버려지지 않고 들여다볼 수 있어야 한다. 파이프라인의 가치는 과정의 가시성에 있다.
3. **단순함을 지키는가** — 오케스트레이터는 조율만, 에이전트는 단일 호출만. 추상화·프레임워크는 실제 필요가 증명되기 전까지 도입하지 않는다.
4. **확장이 쉬운가** — 새 에이전트는 `BaseAgent`를 상속해 추가하고 파이프라인에 끼워넣는 것으로 끝나야 한다.

## 하지 않는 것

- 에이전트 안에 조율 로직을 넣지 않는다 (조율은 오케스트레이터의 일).
- 동작하지 않는 추상 계층을 미리 만들지 않는다.
- 비밀 키를 코드·문서·커밋에 남기지 않는다 (`.env`만 사용, 항상 gitignore).
